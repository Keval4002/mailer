# See `agent.md`

The canonical agent instructions for this repo are in [agent.md](./agent.md).

That file includes:

- GitHub / `gh-personal` usage
- server deployment notes
- Gmail OAuth setup
- bulk API contract
- one-call thread examples with `client_job_id` and `parent_job_client_id`
- reply-stop behavior and thread-specific caveats
